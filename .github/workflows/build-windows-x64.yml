name: build-windows-x64
on:
  push:
  workflow_call:
jobs:
  build:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v3
    - run: cmake -S . -B build
    - run: cmake --build build