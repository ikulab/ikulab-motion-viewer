name: build-linux-x64
on: [push]
jobs:
  install-dependencies:
    runs-on: ubuntu-latest
    steps:
      # - name: checkout code
      #   uses: actions/checkout@v3

      # - name: install vulkan
      #   run: sudo apt install libvulkan-dev -y
      # - name: install vulkan-validation-layers
      #   run: sudo apt install vulkan-validationlayers-dev -y
      # - name: install spirv-tools
      #   run: sudo apt install spirv-tools -y

      # - name: install glfw3
      #   run: sudo apt install libglfw3-dev -y
      # - name: install glm
      #   run: sudo apt install libglm-dev -y

      # - name: install Xxf and Xi
      #   run: sudo apt install libxxf86vm-dev libxi-dev -y
      
      - name: install glslc
        run: |
          ls
          mkdir ./.github/workflows/tmp
          cd ./.github/workflows/tmp
          curl https://storage.googleapis.com/shaderc/artifacts/prod/graphics_shader_compiler/shaderc/linux/continuous_gcc_release/382/20220602-094524/install.tgz --output glslc_install.tgz
          tar -xf glslc_install.tgz
          sudo cp glslc_install/bin/glslc /usr/local/bin/
          cd ..
          rm -rf tmp